import { isMoldy } from '../../../helpers/is-moldy';
import { module, test } from 'qunit';

module('Unit | Helper | is moldy');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isMoldy(42);
  assert.ok(result);
});
