<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">{{#link-to 'index'}}Moldy Mango{{/link-to}}</a>
    </div>
    <div>
      <ul class="nav navbar-nav">
        <li class="active"><a href="#">{{#link-to 'index'}}Home{{/link-to}}</a></li>
        <li><a href="#" id='map-link'>{{#link-to 'theaters' }}Theaters{{/link-to}}</a></li>
        <li><a href="#">Page 2</a></li>
        <li><a href="#">Page 3</a></li>
      </ul>
    </div>
  </div>
</nav>
<div class='container'>
  {{#if session.isAuthenticated}}
    Logged in as {{session.currentUser.displayName}}
    <button {{action "signOut"}}>Sign out</button>
  {{else}}
    {{sign-up signUp="signUp"}}<br>
    {{sign-in signIn="signIn"}}
  {{/if}}

  {{outlet}}
</div>
