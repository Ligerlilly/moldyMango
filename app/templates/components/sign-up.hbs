{{yield}}
{{#if newUser}}
  <form>
    <div class='form-group'>
      <label>Enter Email:</label>
      {{input value=email id='email' class='form-control' autofocus="autofocus"}}
    </div>
    <div class='form-group'>
      <label>Enter Password:</label>
      {{input value=password id='password' type='password' class='form-control'}}
    </div>
    <button class='btn btn-success' {{action 'signUp'}}>Save</button>
  </form>
{{else}}
   <button class='btn btn-info' {{action 'showForm'}}>Sign Up</button>
{{/if}}
